# BarkPark Production Environment Configuration
# These values should be set in Railway dashboard, not in a file
# This file serves as documentation for required production variables

# Environment
NODE_ENV=production
RAILWAY_ENVIRONMENT=production
# PORT is automatically set by Railway

# Database Configuration
# Railway automatically provides DATABASE_URL when you add PostgreSQL
# DATABASE_URL=postgresql://postgres:password@host.railway.app:5432/railway

# JWT Configuration (use strong, unique secret)
JWT_SECRET=your_production_jwt_secret_minimum_32_characters_long_never_share
JWT_EXPIRES_IN=7d

# Admin Access (use strong, unique key)
ADMIN_KEY=your_production_admin_key_for_protected_endpoints_keep_secure

# AWS S3 Configuration (production bucket)
AWS_ACCESS_KEY_ID=your_production_aws_access_key
AWS_SECRET_ACCESS_KEY=your_production_aws_secret_key
AWS_REGION=us-east-1
S3_BUCKET_NAME=barkpark-images

# Email Configuration (production email service)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=apikey
SMTP_PASS=your_production_sendgrid_api_key
SMTP_FROM=hello@barkpark.app

# Application URL (your production URL)
APP_URL=https://barkpark.app

# Security Headers
CORS_ORIGIN=https://barkpark.app

# Monitoring (highly recommended for production)
SENTRY_DSN=your_production_sentry_dsn
# LOG_LEVEL=info

# Performance
# DB_POOL_MIN=2
# DB_POOL_MAX=10

# Never store these in code:
# - Database credentials
# - API keys
# - JWT secrets
# - Admin keys
# - Any sensitive data