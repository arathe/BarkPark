# BarkPark Staging Environment Configuration
# These values should be set in Railway dashboard, not in a file
# This file serves as documentation for required staging variables

# Environment
NODE_ENV=staging
RAILWAY_ENVIRONMENT=staging
# PORT is automatically set by Railway

# Database Configuration
# Railway automatically provides DATABASE_URL when you add PostgreSQL
# DATABASE_URL=postgresql://postgres:password@host.railway.app:5432/railway

# JWT Configuration (use different secret than production)
JWT_SECRET=your_staging_jwt_secret_minimum_32_characters_long
JWT_EXPIRES_IN=7d

# Admin Access (use different key than production)
ADMIN_KEY=your_staging_admin_key_for_protected_endpoints

# AWS S3 Configuration
# Option 1: Use same bucket with folder prefix
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_REGION=us-east-1
S3_BUCKET_NAME=barkpark-images-staging

# Option 2: Use subfolder in production bucket (configure in code)
# S3_BUCKET_NAME=barkpark-images
# S3_FOLDER_PREFIX=staging/

# Email Configuration
# Use real SMTP for staging to test email flow
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=apikey
SMTP_PASS=your_sendgrid_api_key
SMTP_FROM=staging@barkpark.us

# Application URL (your Railway staging URL)
APP_URL=https://barkpark-staging.up.railway.app

# Optional Features
# LOG_LEVEL=debug
# ENABLE_DEBUG_ENDPOINTS=true
# SENTRY_DSN=your_staging_sentry_dsn

# Test Accounts (document but don't store passwords here)
# TEST_USER_EMAIL=testuser@barkpark.us
# TEST_ADMIN_EMAIL=testadmin@barkpark.us